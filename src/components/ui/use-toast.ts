
// This file is intentionally left blank. It is being used by the toast component.
// We are writing it to ensure that the file exists and the build process does not fail.
import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };

